# 贡献指南

感谢你愿意为 **AI Terms Database** 贡献词条！本仓库是 AI Dictionary 客户端的远程在线词库，词条质量直接影响所有用户的体验，因此请在提交前仔细阅读本指南。

## 一、贡献方式

### 1. 提议新词条（Issue）

如果你**不想直接改代码**，可以先提交 Issue：

- 新建 Issue → 选择 **建议新增词条** 模板；
- 填写英文名称、中文名称、建议分类、建议难度、收录理由和参考资料；
- 维护者会评估后决定是否收录，或请你提交 PR。

### 2. 报告词条错误（Issue）

发现词条内容过时、翻译不自然、分类不当或格式问题，请选择 **词条错误报告** 模板提交，尽量附上权威参考链接。

### 3. 直接提交词条（Pull Request）

流程如下：

1. **Fork** 本仓库到你的账号；
2. 在你的 fork 上新建分支（如 `add/new-terms`）；
3. 编辑 `terms.json`，在数组**末尾追加**新词条；
4. 同步更新 `version.json` 的 `version`（次版本 +1）、`update_time`（当天）和 `terms_count`（实际总数）；
5. 本地运行校验脚本（见下文）；
6. 发起 Pull Request，合并目标为 `main`；
7. 通过自动校验（CI）和维护者审核后合并。

## 二、词条 JSON 格式

`terms.json` 是一个 JSON 数组，每个词条必须包含以下 8 个字段：

| 字段 | 说明 | 要求 |
|---|---|---|
| `english_name` | 英文标准名称 | 非空字符串；全局唯一（不区分大小写） |
| `chinese_name` | 中文名称 | 非空字符串，翻译自然、符合习惯 |
| `category` | 分类 | 非空字符串，优先使用下方稳定分类 |
| `difficulty` | 难度 | 整数 `1`（入门）/ `2`（进阶）/ `3`（专家） |
| `short_description` | 一句话解释 | 非空，1～2 句，能让初学者立刻明白 |
| `detail_description` | 详细解释 | 非空，3～5 句；说明"是什么、解决什么问题、基本原理" |
| `application` | 应用场景 | 非空字符串数组，2～3 个真实场景 |
| `related_terms` | 相关术语 | 非空字符串数组，2～3 个，尽量使用仓库已有的 `english_name` |

示例：

```json
{
  "english_name": "Agentic RAG",
  "chinese_name": "智能体 RAG",
  "category": "RAG 与检索",
  "difficulty": 2,
  "short_description": "让智能体自主规划检索策略的 RAG 增强形态。",
  "detail_description": "Agentic RAG 不再单次检索后直接回答，而是由 LLM 根据问题自主决定检索计划、选择工具、评估结果，必要时多轮检索或切换数据源，从而显著提升复杂问题的回答质量。",
  "application": ["复杂多跳问答", "企业知识库助理", "研究资料综述"],
  "related_terms": ["RAG", "AI Agent", "Tool Calling"]
}
```

## 三、稳定分类

目前仓库使用的分类（按词条数量从多到少）：

| 分类 | | 分类 |
|---|---|---|
| 工具与框架 | 模型训练 | 机器学习 |
| AI Agent | AI 伦理与安全 | RAG 与检索 |
| 大语言模型 | NLP 自然语言处理 | 评估与评测 |
| 工具与应用 | 最新趋势 | 推理与部署 |
| 生成式 AI | AI 基础概念 | 数据工程 |
| 硬件与算力 | 深度学习 | 多模态 AI |
| 工程与开发 | 强化学习 | 计算机视觉 |
| 提示工程 | 基础概念 | 迁移学习 |
| AI 应用 | 编程语言 | |

如果新词条确实不属于任何现有分类，可以在 PR 中说明并新增分类，但请勿为了新词频繁改动分类体系。

## 四、内容质量要求

- **英文准确**：使用业界公认的标准写法，不用缩写变体、不造词；
- **中文自然**：面向 AI 零基础用户，同时保留专业含义；
- **不复制百科**：用自己的话解释，避免照搬原文和空泛套话；
- **给出应用**：每个词条必须有真实、具体的应用场景；
- **关联概念**：`related_terms` 尽量指向本词库已有词条，方便用户扩展学习。

## 五、去重要求

提交前务必在仓库中搜索确认，**不允许**以下情况：

- `english_name` 与已有词条重复（不区分大小写）；
- 同义词重复收录（如已收录 `GPT`，就不应再加 `GPT Model` / `GPT AI`）；
- 无意义变体（如只加空格、大小写或中划线不同的写法）；
- 除非确实是不同概念，否则不收录近似词。

## 六、本地校验

提交前请先运行校验脚本（需要 Python 3，无第三方依赖）：

```bash
python scripts/validate_terms.py
```

输出应包含 `校验通过` 且退出码为 0。GitHub Actions 会自动执行同样的校验，校验失败的 PR 无法合并。

## 七、PR 检查清单

- [ ] 已运行 `python scripts/validate_terms.py` 且通过；
- [ ] `terms.json` 为合法 JSON，8 个字段齐全；
- [ ] `english_name` 无重复；
- [ ] `difficulty` 为 1/2/3；
- [ ] `application` 和 `related_terms` 为非空字符串数组；
- [ ] 内容为原创中文解释，未复制百科；
- [ ] 未删除或修改已有词条（除非修复明显错误，且需在 PR 中说明）；
- [ ] 已同步更新 `version.json` 的 `version` / `update_time` / `terms_count`。

## 八、版本更新规则

- 每次发布新词库，`version.json` 的 `version` 必须**提升**（如 1.2.0 → 1.3.0），否则客户端不会触发同步；
- `update_time` 填发布日期，`terms_count` 必须等于 `terms.json` 的实际词条数；
- 词库只做**增量追加**，不删除旧词条，保证所有用户数据安全。

再次感谢你的贡献！
